# GroundedAI local environment variables (copy to .env)
#
# Required:
OPENAI_API_KEY=
#
# Optional (defaults are fine for most cases):
# MODEL=gpt-4o-mini
# EMBEDDING_MODEL=text-embedding-3-small
# CHUNK_SIZE=800
# CHUNK_OVERLAP=100